services:
  # ------------------------
  # RAILS APP
  # ------------------------
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: givingshelf-web
    container_name: givingshelf-web
    env_file:
      - .env
    environment:
      RAILS_ENV: production
      REDIS_URL: redis://redis:6379/0
    depends_on:
      redis:
        condition: service_started
    restart: unless-stopped
    command: bundle exec puma -C config/puma.rb
    expose:
      - "3000"
    volumes:
      - rails_storage:/rails/storage
      - rails_logs:/rails/log
    networks:
      - givingshelf

  # ------------------------
  # DELAYED JOB WORKER
  # ------------------------
  worker:
    image: givingshelf-web
    container_name: givingshelf-worker
    env_file:
      - .env
    environment:
      RAILS_ENV: production
      REDIS_URL: redis://redis:6379/0
    depends_on:
      redis:
        condition: service_started
      web:
        condition: service_started
    restart: unless-stopped
    command: bin/jobs
    networks:
      - givingshelf
  # ------------------------
  # REDIS (CHAT + JOBS)
  # ------------------------
  redis:
    image: redis:7
    container_name: givingshelf-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    networks:
      - givingshelf

  # ------------------------
  # NGINX REVERSE PROXY
  # ------------------------
  nginx:
    image: nginx:stable-alpine
    container_name: givingshelf-nginx
    depends_on:
      web:
        condition: service_started
    ports:
      - "80:80"
      - "443:443"
    restart: unless-stopped
    volumes:
      - ./deploy/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./deploy/nginx/certbot:/etc/letsencrypt
      - ./deploy/nginx/www:/var/www/certbot
    networks:
      - givingshelf

  # ------------------------
  # CERTBOT (SSL CERTIFICATES)
  # ------------------------
  certbot:
    image: certbot/certbot
    container_name: givingshelf-certbot
    volumes:
      - ./deploy/nginx/certbot:/etc/letsencrypt
      - ./deploy/nginx/www:/var/www/certbot
    networks:
      - givingshelf
    # This service runs on-demand, not as a persistent service
    profiles:
      - certbot
# ------------------------
# PERSISTENT VOLUMES
# ------------------------
volumes:
  redis_data:
  rails_storage:
  rails_logs:

networks:
  givingshelf:
    driver: bridge